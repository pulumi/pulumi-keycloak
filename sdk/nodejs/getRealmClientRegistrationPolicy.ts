// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Use this data source to look up an existing client registration policy in a realm by name,
 * optionally narrowing the match by `providerId` and/or `subType`. This is primarily useful
 * for discovering the server-generated ID of a default policy Keycloak auto-creates (e.g.
 * "Trusted Hosts", "Max Clients Limit") so it can be referenced elsewhere.
 *
 * If no policy matches, or more than one policy matches, an error is returned.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as keycloak from "@pulumi/keycloak";
 *
 * const realm = new keycloak.Realm("realm", {realm: "my-realm"});
 * const trustedHosts = keycloak.getRealmClientRegistrationPolicyOutput({
 *     realmId: realm.id,
 *     name: "Trusted Hosts",
 *     providerId: "trusted-hosts",
 *     subType: "anonymous",
 * });
 * export const trustedHostsPolicyId = trustedHosts.apply(trustedHosts => trustedHosts.id);
 * ```
 */
export function getRealmClientRegistrationPolicy(args: GetRealmClientRegistrationPolicyArgs, opts?: pulumi.InvokeOptions): Promise<GetRealmClientRegistrationPolicyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("keycloak:index/getRealmClientRegistrationPolicy:getRealmClientRegistrationPolicy", {
        "name": args.name,
        "providerId": args.providerId,
        "realmId": args.realmId,
        "subType": args.subType,
    }, opts);
}

/**
 * A collection of arguments for invoking getRealmClientRegistrationPolicy.
 */
export interface GetRealmClientRegistrationPolicyArgs {
    /**
     * The name of the policy to find.
     */
    name: string;
    /**
     * Filter by provider ID (e.g. `trusted-hosts`, `max-clients`).
     */
    providerId?: string;
    /**
     * The realm the policy belongs to.
     */
    realmId: string;
    /**
     * Filter by sub-type (`anonymous` or `authenticated`).
     */
    subType?: string;
}

/**
 * A collection of values returned by getRealmClientRegistrationPolicy.
 */
export interface GetRealmClientRegistrationPolicyResult {
    /**
     * A map of the policy's provider-specific configuration values.
     */
    readonly config: {[key: string]: string};
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly name: string;
    readonly providerId: string;
    readonly realmId: string;
    readonly subType: string;
}
/**
 * Use this data source to look up an existing client registration policy in a realm by name,
 * optionally narrowing the match by `providerId` and/or `subType`. This is primarily useful
 * for discovering the server-generated ID of a default policy Keycloak auto-creates (e.g.
 * "Trusted Hosts", "Max Clients Limit") so it can be referenced elsewhere.
 *
 * If no policy matches, or more than one policy matches, an error is returned.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as keycloak from "@pulumi/keycloak";
 *
 * const realm = new keycloak.Realm("realm", {realm: "my-realm"});
 * const trustedHosts = keycloak.getRealmClientRegistrationPolicyOutput({
 *     realmId: realm.id,
 *     name: "Trusted Hosts",
 *     providerId: "trusted-hosts",
 *     subType: "anonymous",
 * });
 * export const trustedHostsPolicyId = trustedHosts.apply(trustedHosts => trustedHosts.id);
 * ```
 */
export function getRealmClientRegistrationPolicyOutput(args: GetRealmClientRegistrationPolicyOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRealmClientRegistrationPolicyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("keycloak:index/getRealmClientRegistrationPolicy:getRealmClientRegistrationPolicy", {
        "name": args.name,
        "providerId": args.providerId,
        "realmId": args.realmId,
        "subType": args.subType,
    }, opts);
}

/**
 * A collection of arguments for invoking getRealmClientRegistrationPolicy.
 */
export interface GetRealmClientRegistrationPolicyOutputArgs {
    /**
     * The name of the policy to find.
     */
    name: pulumi.Input<string>;
    /**
     * Filter by provider ID (e.g. `trusted-hosts`, `max-clients`).
     */
    providerId?: pulumi.Input<string | undefined>;
    /**
     * The realm the policy belongs to.
     */
    realmId: pulumi.Input<string>;
    /**
     * Filter by sub-type (`anonymous` or `authenticated`).
     */
    subType?: pulumi.Input<string | undefined>;
}
