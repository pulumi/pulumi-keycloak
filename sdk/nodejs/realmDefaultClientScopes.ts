// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Allows you to manage the set of default client scopes for a Keycloak realm, which are used when new clients are created.
 *
 * Note that this resource attempts to be an **authoritative** source over the default client scopes for a Keycloak realm,
 * so any Keycloak defaults and manual adjustments will be overwritten.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as keycloak from "@pulumi/keycloak";
 *
 * const realm = new keycloak.Realm("realm", {
 *     realm: "my-realm",
 *     enabled: true,
 * });
 * const clientScope = new keycloak.openid.ClientScope("client_scope", {
 *     realmId: realm.id,
 *     name: "test-client-scope",
 * });
 * const defaultScopes = new keycloak.RealmDefaultClientScopes("default_scopes", {
 *     realmId: realm.id,
 *     defaultScopes: [
 *         "profile",
 *         "email",
 *         "roles",
 *         "web-origins",
 *         clientScope.name,
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * This resource does not support import. Instead of importing, feel free to create this resource
 *
 * as if it did not already exist on the server.
 */
export class RealmDefaultClientScopes extends pulumi.CustomResource {
    /**
     * Get an existing RealmDefaultClientScopes resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RealmDefaultClientScopesState, opts?: pulumi.CustomResourceOptions): RealmDefaultClientScopes {
        return new RealmDefaultClientScopes(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'keycloak:index/realmDefaultClientScopes:RealmDefaultClientScopes';

    /**
     * Returns true if the given object is an instance of RealmDefaultClientScopes.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RealmDefaultClientScopes {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RealmDefaultClientScopes.__pulumiType;
    }

    /**
     * An array of default client scope names that should be used when creating new Keycloak clients.
     */
    declare public readonly defaultScopes: pulumi.Output<string[]>;
    /**
     * The realm this client and scopes exists in.
     */
    declare public readonly realmId: pulumi.Output<string>;

    /**
     * Create a RealmDefaultClientScopes resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RealmDefaultClientScopesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RealmDefaultClientScopesArgs | RealmDefaultClientScopesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RealmDefaultClientScopesState | undefined;
            resourceInputs["defaultScopes"] = state?.defaultScopes;
            resourceInputs["realmId"] = state?.realmId;
        } else {
            const args = argsOrState as RealmDefaultClientScopesArgs | undefined;
            if (args?.defaultScopes === undefined && !opts.urn) {
                throw new Error("Missing required property 'defaultScopes'");
            }
            if (args?.realmId === undefined && !opts.urn) {
                throw new Error("Missing required property 'realmId'");
            }
            resourceInputs["defaultScopes"] = args?.defaultScopes;
            resourceInputs["realmId"] = args?.realmId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RealmDefaultClientScopes.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RealmDefaultClientScopes resources.
 */
export interface RealmDefaultClientScopesState {
    /**
     * An array of default client scope names that should be used when creating new Keycloak clients.
     */
    defaultScopes?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The realm this client and scopes exists in.
     */
    realmId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a RealmDefaultClientScopes resource.
 */
export interface RealmDefaultClientScopesArgs {
    /**
     * An array of default client scope names that should be used when creating new Keycloak clients.
     */
    defaultScopes: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The realm this client and scopes exists in.
     */
    realmId: pulumi.Input<string>;
}
