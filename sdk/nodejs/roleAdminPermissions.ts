// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Allows you to manage a fine-grained admin permission for Keycloak roles.
 *
 * This resource requires Fine-Grained Admin Permissions v2 (`admin-fine-grained-authz:v2`), available since Keycloak 26.2. See the `docker-compose.yml` for an example of how to enable this feature.
 *
 * Each instance of this resource represents **one permission** in Keycloak (matching what you see when you click "Create permission" in the Keycloak UI). A single permission can span multiple scopes, target multiple specific roles, and reference multiple policies.
 *
 * When `adminPermissionsEnabled = true` is set on the realm, Keycloak automatically creates an `admin-permissions` client that serves as the authorization resource server for all FGAPv2 admin permissions.
 *
 * Available scopes:
 *
 * - `map-role` — map this role to users, groups, or clients
 * - `map-role-client-scope` — use this role as a client scope
 * - `map-role-composite` — add this role as a composite to another role
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as keycloak from "@pulumi/keycloak";
 *
 * const realm = new keycloak.Realm("realm", {
 *     realm: "my-realm",
 *     adminPermissionsEnabled: true,
 * });
 * const adminPermissions = keycloak.openid.getClientOutput({
 *     realmId: realm.id,
 *     clientId: "admin-permissions",
 * });
 * const roleA = new keycloak.Role("role_a", {
 *     realmId: realm.id,
 *     name: "role-a",
 * });
 * const roleB = new keycloak.Role("role_b", {
 *     realmId: realm.id,
 *     name: "role-b",
 * });
 * const admins = new keycloak.Group("admins", {
 *     realmId: realm.id,
 *     name: "admins",
 * });
 * const adminsPolicy = new keycloak.openid.ClientGroupPolicy("admins_policy", {
 *     realmId: realm.id,
 *     resourceServerId: adminPermissions.id,
 *     name: "admins-policy",
 *     groups: [{
 *         id: admins.id,
 *         path: admins.path,
 *         extendChildren: false,
 *     }],
 *     logic: "POSITIVE",
 *     decisionStrategy: "UNANIMOUS",
 * });
 * // Permission targeting two specific roles with multiple scopes.
 * const adminsMapSpecificRoles = new keycloak.RoleAdminPermissions("admins_map_specific_roles", {
 *     realmId: realm.id,
 *     name: "admins-can-map-specific-roles",
 *     description: "Admins can map or make composite role-a and role-b",
 *     decisionStrategy: "UNANIMOUS",
 *     roleIds: [
 *         roleA.id,
 *         roleB.id,
 *     ],
 *     scopes: [
 *         "map-role",
 *         "map-role-composite",
 *     ],
 *     policies: [adminsPolicy.id],
 * });
 * // Permission targeting ALL roles in the realm (role_ids omitted).
 * const adminsMapAnyRole = new keycloak.RoleAdminPermissions("admins_map_any_role", {
 *     realmId: realm.id,
 *     name: "admins-can-map-any-role",
 *     scopes: ["map-role"],
 *     policies: [adminsPolicy.id],
 * });
 * ```
 *
 * ## Import
 *
 * Role admin permissions can be imported using `{{realmId}}/{{permissionId}}`:
 *
 * ```sh
 * $ pulumi import keycloak:index/roleAdminPermissions:RoleAdminPermissions example my-realm/permission-uuid
 * ```
 *
 * After import, run `pulumi up` to reconcile `roleIds`, `scopes`, and `policies` with your configuration.
 */
export class RoleAdminPermissions extends pulumi.CustomResource {
    /**
     * Get an existing RoleAdminPermissions resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RoleAdminPermissionsState, opts?: pulumi.CustomResourceOptions): RoleAdminPermissions {
        return new RoleAdminPermissions(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'keycloak:index/roleAdminPermissions:RoleAdminPermissions';

    /**
     * Returns true if the given object is an instance of RoleAdminPermissions.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RoleAdminPermissions {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RoleAdminPermissions.__pulumiType;
    }

    /**
     * The ID of the `admin-permissions` client, which acts as the resource server for these permissions.
     */
    declare public /*out*/ readonly authorizationResourceServerId: pulumi.Output<string>;
    /**
     * Decision strategy. One of `UNANIMOUS`, `AFFIRMATIVE`, or `CONSENSUS`. Defaults to `UNANIMOUS`.
     */
    declare public readonly decisionStrategy: pulumi.Output<string | undefined>;
    /**
     * Description of the permission.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Always `true` when the resource exists.
     */
    declare public /*out*/ readonly enabled: pulumi.Output<boolean>;
    /**
     * The name of the permission. Must be unique within the `admin-permissions` resource server. On first apply, if a permission with this name already exists it is adopted; otherwise a new one is created.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The internal Keycloak UUID of the permission.
     */
    declare public /*out*/ readonly permissionId: pulumi.Output<string>;
    /**
     * Set of policy IDs to attach to the permission.
     */
    declare public readonly policies: pulumi.Output<string[] | undefined>;
    /**
     * The realm in which to manage this permission.
     */
    declare public readonly realmId: pulumi.Output<string>;
    /**
     * Set of role UUIDs (`keycloak_role.xxx.id`) this permission applies to. When omitted or empty, the permission applies to **all roles** in the realm.
     */
    declare public readonly roleIds: pulumi.Output<string[] | undefined>;
    /**
     * Set of scopes this permission grants. Valid values: `map-role`, `map-role-client-scope`, `map-role-composite`.
     */
    declare public readonly scopes: pulumi.Output<string[]>;

    /**
     * Create a RoleAdminPermissions resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RoleAdminPermissionsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RoleAdminPermissionsArgs | RoleAdminPermissionsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RoleAdminPermissionsState | undefined;
            resourceInputs["authorizationResourceServerId"] = state?.authorizationResourceServerId;
            resourceInputs["decisionStrategy"] = state?.decisionStrategy;
            resourceInputs["description"] = state?.description;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["name"] = state?.name;
            resourceInputs["permissionId"] = state?.permissionId;
            resourceInputs["policies"] = state?.policies;
            resourceInputs["realmId"] = state?.realmId;
            resourceInputs["roleIds"] = state?.roleIds;
            resourceInputs["scopes"] = state?.scopes;
        } else {
            const args = argsOrState as RoleAdminPermissionsArgs | undefined;
            if (args?.realmId === undefined && !opts.urn) {
                throw new Error("Missing required property 'realmId'");
            }
            if (args?.scopes === undefined && !opts.urn) {
                throw new Error("Missing required property 'scopes'");
            }
            resourceInputs["decisionStrategy"] = args?.decisionStrategy;
            resourceInputs["description"] = args?.description;
            resourceInputs["name"] = args?.name;
            resourceInputs["policies"] = args?.policies;
            resourceInputs["realmId"] = args?.realmId;
            resourceInputs["roleIds"] = args?.roleIds;
            resourceInputs["scopes"] = args?.scopes;
            resourceInputs["authorizationResourceServerId"] = undefined /*out*/;
            resourceInputs["enabled"] = undefined /*out*/;
            resourceInputs["permissionId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RoleAdminPermissions.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RoleAdminPermissions resources.
 */
export interface RoleAdminPermissionsState {
    /**
     * The ID of the `admin-permissions` client, which acts as the resource server for these permissions.
     */
    authorizationResourceServerId?: pulumi.Input<string | undefined>;
    /**
     * Decision strategy. One of `UNANIMOUS`, `AFFIRMATIVE`, or `CONSENSUS`. Defaults to `UNANIMOUS`.
     */
    decisionStrategy?: pulumi.Input<string | undefined>;
    /**
     * Description of the permission.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Always `true` when the resource exists.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the permission. Must be unique within the `admin-permissions` resource server. On first apply, if a permission with this name already exists it is adopted; otherwise a new one is created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The internal Keycloak UUID of the permission.
     */
    permissionId?: pulumi.Input<string | undefined>;
    /**
     * Set of policy IDs to attach to the permission.
     */
    policies?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The realm in which to manage this permission.
     */
    realmId?: pulumi.Input<string | undefined>;
    /**
     * Set of role UUIDs (`keycloak_role.xxx.id`) this permission applies to. When omitted or empty, the permission applies to **all roles** in the realm.
     */
    roleIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Set of scopes this permission grants. Valid values: `map-role`, `map-role-client-scope`, `map-role-composite`.
     */
    scopes?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a RoleAdminPermissions resource.
 */
export interface RoleAdminPermissionsArgs {
    /**
     * Decision strategy. One of `UNANIMOUS`, `AFFIRMATIVE`, or `CONSENSUS`. Defaults to `UNANIMOUS`.
     */
    decisionStrategy?: pulumi.Input<string | undefined>;
    /**
     * Description of the permission.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The name of the permission. Must be unique within the `admin-permissions` resource server. On first apply, if a permission with this name already exists it is adopted; otherwise a new one is created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Set of policy IDs to attach to the permission.
     */
    policies?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The realm in which to manage this permission.
     */
    realmId: pulumi.Input<string>;
    /**
     * Set of role UUIDs (`keycloak_role.xxx.id`) this permission applies to. When omitted or empty, the permission applies to **all roles** in the realm.
     */
    roleIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Set of scopes this permission grants. Valid values: `map-role`, `map-role-client-scope`, `map-role-composite`.
     */
    scopes: pulumi.Input<pulumi.Input<string>[]>;
}
